# Leo AI System - 项目级 AI 行为规范
# ==========================================

# 1. 语言规范 (Language Protocol)
# ------------------------------------------
# 🔴 核心规则：必须全中文思考与交互
# 1. Output (输出): 必须使用简体中文。
# 2. Thought Process (思维链): 必须使用简体中文进行推理、规划和反思。
#    - 禁止使用英文进行 Thinking。
#    - 禁止在 Thought 中中英混杂。
# 3. Comments (代码注释): 必须使用简体中文。

# 2. 工程规范 (Engineering Standards)
# ------------------------------------------
# - 必须遵守 REFACTOR_PLAN.md 中的重构计划。
# - 优先使用 Python 标准包结构 (snake_case)。
# - 严禁引入新的 "ModuleNotFoundError"，修改文件移动后必须通过 `pip install -e .` 验证。

# 3. 系统认知 (System Awareness)
# ------------------------------------------
# - 这是一个"自我进化"的系统，包含元技能 (Meta-Skills)。
# - AI 需要意识到自己是系统的一部分，不仅是执行者，也是构建者。
# - 任何代码修改都应考虑"复利"效应，即提升系统的长期价值。
