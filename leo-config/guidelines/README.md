# 去AI化内容处理指南

## 📖 概述

这个模块提供了一套完整的去AI化内容处理方案，让AI生成的内容更像真人创作。

## 🎯 核心功能

### 1. 禁用词汇检测
自动识别并替换以下类型的词汇：
- 技术词汇（智慧、智能、AI等）
- 营销黑话（赋能、痛点、赛道等）
- 过度完美词汇（完美、极致、顶级等）

### 2. 表达方式转换
- 将正式表达改为口语化
- 将绝对化改为相对化
- 加入个人感受和不确定性

### 3. 质量检查
- 检测禁用词汇使用
- 评估表达自然度
- 提供改进建议

## 🚀 快速使用

### Python代码中使用

```python
from leo_config.guidelines import deaiify, check_text_quality

# 原AI化文本
ai_text = """
该项目采用智慧农贸系统，打造全方位数字化生活服务。
依托14万人口红利，构建核心商业护城河。
投资回报率高达20%，保证6个月回本！
"""

# 去AI化处理
human_text = deaiify(ai_text)
print(human_text)
# 输出: 这个市场挺正规的，买菜付款都方便。
# 周边14万人，这生意肯定差不了。
# 差不多半年能回本，一年赚个20%应该没问题。

# 质量检查
quality = check_text_quality(human_text)
print(f"分数: {quality['score']}, 通过: {quality['passed']}")
```

### 在Skills中使用

```python
from leo_config.guidelines import DeAIifier

class MySkill:
    def __init__(self):
        self.deaiifier = DeAIifier()

    def generate_content(self, topic):
        # 1. 正常生成内容
        content = self._generate(topic)

        # 2. 去AI化处理
        content = self.deaiifier.process(content)

        # 3. 质量检查
        quality = self.deaiifier.check_quality(content)

        if not quality['passed']:
            # 根据建议改进
            suggestions = self.deaiifier.suggest_improvements(content)
            print(f"改进建议: {suggestions}")

        return content
```

## 📋 配置文件

配置位于：`leo-config/guidelines/deaiification_guide.yaml`

### 主要配置项

#### 禁用词汇清单
```yaml
forbidden_words:
  - 智慧
  - 智能
  - AI
  - 赋能
  - 痛点
  - 赛道
```

#### 替换词汇
```yaml
word_replacements:
  "智慧农贸": "标准化农贸市场"
  "投资回报": "能赚多少钱"
  "核心卖点": "主要优势"
```

#### 表达风格
```yaml
expression_style:
  colloquial:
    - "我跟你说"
    - "说实话"
  personal_touch:
    - "我觉得"
    - "我看了看"
  uncertainty:
    - "差不多"
    - "应该"
    - "可能"
```

## 📝 去AI化原则

### 1. 真实性
- 加入个人经历和感受
- 描述具体场景和细节
- 承认不确定性

### 2. 接地气
- 用大白话解释专业术语
- 用具体数字替代抽象概念
- 用实际案例替代理论说教

### 3. 情感共鸣
- 分享真实困惑和担忧
- 展示决策过程的犹豫
- 表达真实的情绪变化

### 4. 不完美
- 承认自己的局限
- 展示失败或挫折
- 分享学习过程

## 🔄 在Leo系统中的应用

### 自动应用

当使用以下Skills时，会自动应用去AI化规则：
- `project-marketing-doc-generator-cskill` - 营销文档生成
- `content-layout-leo-cskill` - 内容排版

### 手动应用

```bash
# 调用系统时自动去AI化
python leo-system.py --execute "创作建华官园营销文案，要去AI化"
```

## 📊 质量评分标准

| 分数 | 等级 | 说明 |
|------|------|------|
| 90-100 | 优秀 | 非常自然，像真人 |
| 70-89 | 良好 | 基本自然，有小问题 |
| 50-69 | 一般 | 有明显AI痕迹 |
| 0-49 | 差 | AI痕迹严重 |

## 🛠️ 开发指南

### 扩展配置

编辑 `deaiification_guide.yaml` 添加新的规则：

```yaml
forbidden_words:
  - "你的禁用词"

word_replacements:
  "AI词汇": "人类词汇"
```

### 自定义处理器

```python
from leo_config.guidelines import DeAIifier

class CustomDeAIifier(DeAIifier):
    def _custom_rule(self, text: str) -> str:
        # 你的自定义规则
        return text

    def process(self, text: str) -> str:
        text = super().process(text)
        return self._custom_rule(text)
```

## 📚 相关资源

- [去AI化指南完整配置](leo-config/guidelines/deaiification_guide.yaml)
- [去AI化处理器源码](leo-config/guidelines/deaiifier.py)
- [营销文档生成器Skill](leo-skills/tools/project-marketing-doc-generator-cskill/)

## 🤝 贡献

如果你发现新的AI化表达模式，欢迎更新配置文件并提交PR！

## 📞 支持

有问题或建议？请联系Leo团队或提Issue。

---

**最后更新**: 2026-01-08
**版本**: 1.0.0
