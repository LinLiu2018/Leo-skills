[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "leo-ai-system"
version = "1.0.0"
description = "Leo AI Agent System - Skills + Subagents 协同工作架构"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Leo Liu", email = "linliu2018@example.com"}
]
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
keywords = ["ai", "agent", "skills", "automation", "orchestrator"]
dependencies = [
    "pyyaml>=6.0",
    "requests>=2.28.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-asyncio>=0.21",
    "black>=23.0",
    "isort>=5.12",
    "flake8>=6.0",
]
web = [
    "streamlit>=1.28",
    "gradio>=4.0",
]
all = [
    "leo-ai-system[dev]",
    "leo-ai-system[web]",
]

[project.scripts]
leo = "leo_system:main"

[project.urls]
Homepage = "https://github.com/LinLiu2018/Leo-skills"
Repository = "https://github.com/LinLiu2018/Leo-skills"
Documentation = "https://github.com/LinLiu2018/Leo-skills/blob/main/LEO_SYSTEM_README.md"

[tool.setuptools.packages.find]
where = ["src"]
include = ["leo_orchestrator*", "leo_subagents*", "leo_skills*", "leo_system*", "leo_interface*", "leo_config*", "leo_knowledge*", "leo_workflows*"]

[tool.setuptools.package-data]
"*" = ["*.yaml", "*.yml", "*.md", "*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.black]
line-length = 100
target-version = ['py39', 'py310', 'py311']
include = '\.pyi?$'

[tool.isort]
profile = "black"
line_length = 100
known_first_party = ["leo_orchestrator", "leo_subagents", "leo_skills"]
